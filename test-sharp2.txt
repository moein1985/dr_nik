const sharp = require("sharp");
const buffer = Buffer.alloc(100*100*3, 128);
sharp(buffer, {raw:{width:100,height:100,channels:3}}).jpeg().toBuffer().then(b=>console.log("sharp OK, size:",b.length)).catch(e=>console.log("FAIL:",e.message));
